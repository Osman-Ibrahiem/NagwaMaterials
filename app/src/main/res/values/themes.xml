<resources>

    <style name="Theme.Launch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
<!--                <item name="windowSplashScreenAnimationDuration">800</item>-->
        <item name="postSplashScreenTheme">@style/Theme.Materials</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Base" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/live_red</item>
        <item name="colorSecondaryVariant">@color/dark_red</item>
        <item name="colorOnSecondary">@color/white</item>
    </style>

    <style name="Theme.Materials" parent="Theme.Base" />
</resources>